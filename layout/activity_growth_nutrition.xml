<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F9FAFB"
    android:layoutDirection="rtl"
    tools:context=".GrowthNutritionActivity">

    <!-- Header -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <ImageView
            android:id="@+id/iv_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_chevron_left"
            android:layout_gravity="start|center_vertical"
            app:tint="@color/text_secondary"
            android:padding="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="مرکز رشد و تغذیه"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"/>
    </FrameLayout>

    <!-- Segmented Control (TabLayout) -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tab_layout_growth_nutrition"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/segmented_control_background"
        app:tabGravity="fill"
        app:tabIndicatorHeight="0dp"
        app:tabMode="fixed"
        app:tabRippleColor="@null"
        app:tabSelectedTextColor="@color/girl_button"
        app:tabTextAppearance="@style/SegmentedTabTextAppearance"
        app:tabBackground="@drawable/segmented_control_item_selector" />

    <!-- ViewPager2 to host fragments -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/view_pager_growth_nutrition"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
