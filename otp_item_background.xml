<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- حالتی که فیلد انتخاب شده (focused) -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/white" />
            <corners android:radius="12dp" />
            <stroke android:width="2dp" android:color="@color/default_button" />
        </shape>
    </item>

    <!-- حالت عادی -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/default_background_light" />
            <corners android:radius="12dp" />
            <stroke android:width="1dp" android:color="@color/edit_text_border" />
        </shape>
    </item>
</selector>
