// فایل build.gradle در سطح پروژه (Project-level)
// این فایل را با build.gradle در پوشه app اشتباه نگیرید.

plugins {
    // تعریف پلاگین اپلیکیشن اندروید با نسخه مشخص
    id 'com.android.application' version '8.2.0' apply false
    // تعریف پلاگین کاتلین برای اندروید
    id 'org.jetbrains.kotlin.android' version '1.9.0' apply false
    id("androidx.navigation.safeargs.kotlin") version "2.7.7" apply false
}