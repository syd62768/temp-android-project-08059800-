<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- دسترسی‌های مورد نیاز برنامه -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HamraheKoodak"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MealPlannerActivity" android:exported="false" />
        <activity android:name=".NutritionActivity" android:exported="false" />
        <activity android:name=".GrowthChartActivity" android:exported="false" />
        <activity android:name=".DashboardActivity" android:exported="false" />

        <!-- ==================== FIX START ==================== -->
        <!--
         خطای کرش به خاطر نبودن این بخش بود.
         اکتیویتی جدید باید در اینجا ثبت شود.
        -->
        <activity
            android:name=".GrowthNutritionActivity"
            android:exported="false" />
        <!-- ===================== FIX END ===================== -->

    </application>

</manifest>

